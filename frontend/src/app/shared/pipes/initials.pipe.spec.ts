import { InitialsPipePipe } from './initials.pipe';

describe('InitialsPipe', () => {
  it('create an instance', () => {
    const pipe = new InitialsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
